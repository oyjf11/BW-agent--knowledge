Use these skills:
- `index-maintainer`
- `note-template-normalizer`
- `vault-audit`

Target Vault path:
- Use `$ARGUMENTS` if provided.
- Otherwise use `AI-Agent-Interview-Knowledge/`.

Before editing:
1. Run `git status`.
2. Output a concise plan.
3. Do not delete files.
4. Do not overwrite existing content.
5. Follow AGENTS.md.

Allowed:
- create missing `_index.md`
- update `_index.md` links
- add missing frontmatter
- add missing required headings