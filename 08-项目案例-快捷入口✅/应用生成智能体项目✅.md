---
type: project
status: done
tags:
  - 项目案例
  - 高频面试
---

# 应用生成智能体项目

## 30 秒版本
基于 LlamaCoder 二次改造的企业级 AI 轻应用生成平台。用户输入自然语言需求 → Router 判断意图 → UIPlanner / SupabasePlanner / RoutingPlanner 三路规划 → Codegen 生成 React + TypeScript + Tailwind 代码 → Supabase 自动建表 → SSO + RLS 租户隔离 → Sandpack 预览调试 → 打包部署。

## 1 分钟版本
我们项目早期基于 LlamaCoder 做二次改造。原版 LlamaCoder 更像开源版 Claude Artifacts，主要解决 prompt 生成 React 小应用和在线预览。我们在这个基础上做了企业化扩展：
- 加入 Router / Planner 多阶段生成，拆出页面生成、Schema 生成、SQL 建表、路由规划和数据绑定
- 平台控制面数据放 MySQL，用户生成应用后的业务数据放 PostgreSQL / Supabase
- 权限接企业 SSO，拿到 user_id 和 tenant_id 后通过 Supabase JWT 和 RLS 做租户隔离
- 前端侧继续保留流式生成和 Sandpack 预览体验，但增加了流式解析、错误反馈和增量修改能力

## 核心流程
```text
用户自然语言需求 → 前端提交生成任务 → 平台后端创建任务记录（MySQL）
  → 调用 Together AI SDK 流式返回 → Router 判断任务类型
  → UIPlanner / SupabasePlanner / RoutingPlanner 分别规划
  → Codegen 生成 React + TS + Tailwind 代码
  → 生成 Schema / SQL → PG/Supabase 创建业务表
  → SSO → JWT → RLS → Sandpack 预览 → 打包部署
```

## 技术亮点
- **多阶段生成**：Router → Planner（UI/Data/Routing）→ Codegen → QA 四阶段
- **控制面/数据面分离**：MySQL 平台元数据 + PG/Supabase 用户业务数据
- **LLM 选型**：Together AI SDK 调用开源代码模型
- **企业权限**：SSO → JWT → Supabase RLS 三层租户隔离
- **动态建表安全**：Schema 白名单 → 模板生成 SQL → dry-run → 自动 RLS
- **流式体验优化**：流式态与预览态分离，buffer 批量刷新，避免 Sandpack 频繁编译
- **增量修改**：project snapshot + Schema diff + target_files 限定修改范围

## 产品价值
- 降低企业内部轻应用开发门槛：自然语言 → 可运行应用
- 企业级能力补齐：权限、多租户、版本、审计
- 可控性：多阶段拆分 + 边界约束 + 自动验证

## 可映射知识节点
- [[24.1 核心链路✅]] - 生成流水线
- [[24.2 Router深水区✅]] - 意图识别
- [[24.3 Planner深水区✅]] - 多阶段规划
- [[24.4 生成稳定性✅]] - 流式优化
- [[24.5 企业级能力✅]] - 企业化改造
- [[31.1 身份认证✅]] - SSO + JWT
- [[31.3 多租户隔离✅]] - Supabase RLS
- [[31.4 数据权限✅]] - using / with check

## 高频追问
1. 为什么选 LlamaCoder 和 Together AI 而不是自研？
2. Router 和 Planner 的区别是什么？
3. 动态建表怎么保证安全？
4. 租户隔离怎么做？为什么不只在前端过滤？
5. 流式生成时 Sandpack 卡顿怎么优化？
6. 和原版 LlamaCoder 到底有什么区别？
7. 为什么平台数据存 MySQL，用户数据存 PG/Supabase？
