Use the `source-to-resume-bullets` skill.

Input:
- `$1` = source repo path
- `$2` = project name

Target output:
`AI-Agent-Interview-Knowledge/11-源码证据库/$2-简历素材.md`

Rules:
1. Do not invent metrics.
2. Separate safe / needs evidence / not recommended.
3. Every strong bullet should include source evidence.
4. Follow AGENTS.md.