Use the `link-graph-builder` skill.

Target Vault path:
- Use `$ARGUMENTS` if provided.
- Otherwise use `AI-Agent-Interview-Knowledge/`.

Rules:
1. Prefer linking to existing notes.
2. Do not create new notes unless explicitly necessary.
3. Add at most 5 new links per file.
4. Follow AGENTS.md.